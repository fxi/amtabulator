% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabulator.R
\name{tabulator_replace_data}
\alias{tabulator_replace_data}
\title{Replace all data in a Tabulator table}
\usage{
tabulator_replace_data(proxy, data)
}
\arguments{
\item{proxy}{A Tabulator proxy object created by \code{tabulator_proxy()}}

\item{data}{A data frame containing the new data to display in the table}
}
\value{
None (called for side effect)
}
\description{
This function completely replaces the existing data in a Tabulator table with new data.
Unlike \code{tabulator_update_data}, this function replaces the entire dataset at once
rather than updating it in chunks.
}
\examples{
\dontrun{
# In server function
proxy <- tabulator_proxy("table_id")
new_data <- data.frame(
  name = c("John", "Jane"),
  age = c(25, 30)
)
tabulator_replace_data(proxy, new_data)
}

}
